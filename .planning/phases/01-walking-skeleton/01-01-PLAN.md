# Phase 1 Plan: Bootstrap Walking Skeleton

**Status:** Complete
**Requirements:** UX-01, UX-02, UX-03, BACKEND-02, BACKEND-03, CONFIG-02, CONFIG-03, CONFIG-04, DOCS-01, DOCS-02,
QUAL-01, QUAL-02, QUAL-04, QUAL-05

## Goal

Create the first runnable Loopwire foundation with project planning, pure domain behavior, app shell, docs/site,
CI/deploy workflows, and validation commands.

## Tasks

- [x] Create GSD project, requirements, roadmap, state, and phase context.
- [x] Add pnpm workspace and pinned package/tool versions verified from registries.
- [x] Implement pure core package for backend decisioning, configuration switching, hidden monitors, and persistence.
- [x] Add Vitest coverage for core behavior.
- [x] Build the first Svelte desktop UI shell with backend/settings/configuration/monitor interactions.
- [x] Add Tauri 2 shell metadata for native desktop integration.
- [x] Add VitePress docs/site with above-the-fold product page, screenshot asset, install docs, backend docs, and
  start-on-boot docs.
- [x] Add CI, continuous host-test entry point, Bunny.net deploy workflow, installer script, CT script, and secret helper.
- [x] Run install, typecheck, tests, app build, docs build, and script syntax checks.

## Verification

- `pnpm install` passed.
- `pnpm test` passed: 3 core test files, 10 tests.
- `pnpm typecheck` passed for core, docs, and desktop.
- `pnpm build` passed for core, VitePress docs, and desktop Vite app.
- `pnpm check` passed.
- `pnpm verify:scripts` passed.
- Workflow YAML parsed with Ruby `YAML.load_file`; `actionlint` was not installed.
- `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` passed.

## Risks

- Tauri Linux builds may need host WebKit/GTK packages on other machines. This host had enough dependencies for
  `cargo check`; packaging smoke tests remain Phase 5/7 work.
- VitePress depends on its own Vite version. Keep docs isolated under `@loopwire/docs`.
- Nix is not installed on this host, so `flake.nix` is present but not lockfile-verified.
