# Phase 2 Plan: Backend Capability Detection

**Status:** Complete
**Requirements:** BACKEND-01, BACKEND-02, BACKEND-03, BACKEND-04, BACKEND-05, QUAL-02, QUAL-03

## Goal

Add a real, read-only backend capability layer for PipeWire, PulseAudio, JACK, and ALSA while preserving architecture
boundaries and avoiding fake routing claims.

## Tasks

- [x] Add `@loopwire/audio-host` workspace package.
- [x] Define typed backend capability reports, diagnostics, command runner contract, and detector result.
- [x] Implement read-only detectors for PipeWire, PulseAudio, JACK, and ALSA using injected command execution.
- [x] Add fake-runner tests covering available, missing, inactive, compatibility, and failure states.
- [x] Add a CLI script that prints backend detection JSON.
- [x] Update CT diagnostics to include the JSON detector output.
- [x] Update docs to explain the detection contract and current non-routing boundary.
- [x] Run root validation, backend tests, script syntax checks, and a read-only host diagnostic command.

## Verification

- `pnpm --filter @loopwire/audio-host test` passed: 1 file, 5 tests.
- `pnpm check` passed: typecheck, tests, and builds across workspace.
- `pnpm verify:scripts` passed.
- `pnpm detect:audio` passed and reported host backend availability.
- `bash scripts/ct-host-check.sh` passed with redacted diagnostics.
- `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` passed.
- Workflow YAML parse and `git diff --check` passed.

## Risks

- Host commands vary by distro. Detection must tolerate absent commands and inactive services.
- ALSA devices may be absent on containers or CI. That should be reported as unavailable, not failed validation.
- This phase does not prove routing. It only proves detection and capability reporting.
- `actionlint` is still not installed on this host; workflow validation used YAML parsing only.
