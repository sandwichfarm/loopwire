# Plan 15.1 — Power-User Surfaces (SEED-002, SURF-01..03)

## Context

- Read `AGENTS.md`, `.planning/seeds/SEED-002-restore-power-user-surfaces.md`.
- Export/import domain API: `exportConfiguration`/`importConfiguration` in `packages/core/src/persistence.ts` (already exported from `@loopwire/core`).
- Settings dialog: `apps/desktop/src/lib/components/SettingsWindow.svelte` (sections pattern; runtime stores from `apps/desktop/src/lib/services/runtime.ts` — `capabilityReports`, `detectionNote`, `activity`).
- Host bindings domain API: `setEndpointDeviceName` in core (currently unused by UI). Card Options strips: `SourceCard.svelte`, `MonitorCard.svelte`, `BusBlock.svelte` (bus has no Options strip today).
- Store mutation pattern: `apps/desktop/src/lib/stores/deviceStore.ts` (`mutate`, typed results); toasts via `uiStore.pushToast`.

## Tasks

1. **Export/Import (SURF-01)**: new Settings section "Transfer": Export button copies the active device's export JSON into a readonly textarea (and clipboard when available); Import textarea + button runs `importConfiguration` via a new `deviceStore.importDevice(raw)` (returns typed result; inserts + selects the imported device). Failure → error toast, state untouched. Unit tests in deviceStore.test.ts (import valid/invalid round-trip using exportConfiguration).
2. **Diagnostics (SURF-02)**: Settings "Diagnostics" disclosure under the Audio Backend section listing each capability report from `runtimeService.capabilityReports`: kind, availability, mixing scope, per-op states, and diagnostics messages. Read-only; no new probes. Render gracefully when reports are empty (browser preview).
3. **Manual host bindings (SURF-03)**: add a "Host binding" text field to the Options strip of SourceCard and MonitorCard, and add an Options strip to BusBlock containing the same field. Wire to a new `deviceStore.setHostBinding(deviceId, endpointId, deviceName)` using core `setEndpointDeviceName` (empty clears). Placeholder text per role like the pre-rebuild UI (`pw-link/JACK source port`, `pw-link/JACK target port`, `alsa_output...`). Structural change → call `reapplySelectedDevice()` from `apps/desktop/src/lib/app.ts` after a successful binding edit.
4. **Docs**: update `apps/docs/docs/guide/configurations.md` — replace the "Manual host-binding overrides are not part of the rebuilt UI yet (tracked as a UI gap)" passage with the new behavior, keeping `pnpm verify:docs` green (update `scripts/verify-docs.sh` needles "Manual host-binding overrides are not part of the" and "tracked as a UI gap" to new phrases in the same commit). README "Configuration export/import and the on-demand diagnostics panel were removed..." sentence must be rewritten too. Release-notes bullet.

## Verification

- `pnpm --filter @loopwire/desktop test && pnpm --filter @loopwire/desktop typecheck && pnpm verify:docs && pnpm verify:requirements`
- Playwright smoke on the dev server: open Settings → export shows JSON; import a tweaked copy → new device appears; host-binding field visible in a source card's Options.

## Commit

One commit `Restore export/import, diagnostics, and manual host bindings`; update ROADMAP Phase 15 row + REQUIREMENTS SURF-01..03; write `15.1-SUMMARY.md`.
