# Plan 14.1 — Multichannel Bus Creation (SEED-004, BUS-01)

## Context

- Read `AGENTS.md`, `.planning/seeds/SEED-004-multichannel-bus-creation.md`.
- Current bus add: `addBus` in `apps/desktop/src/lib/stores/deviceStore.ts` (hardcoded stereo, `nextBusLabel`), triggered instantly from `ColumnHeader` in `apps/desktop/src/lib/components/DeviceCanvas.svelte` (`addAction="instant"`).
- Channel labels: `channelLabel` in deviceStore; adapter layouts: `audioPositionsForChannels` in `packages/audio-host/src/pipewire-adapter.ts` (already supports 1–8).
- Menu component: `apps/desktop/src/lib/components/PopupMenu.svelte`.

## Tasks

1. **Store**: extend `addBus(deviceId, channels = 2)`; `nextBusLabel(outputs, channels)` labels: 1ch → `Channel N`, 2ch → `Channels N & M`, 4ch → `Channels N–M`. Start channel remains cumulative. Update/extend `deviceStore.test.ts` (mono/quad labels, start channels, auto-select).
2. **UI**: Output Channels ⊕ becomes a small menu (PopupMenu) with items Stereo Bus (default/first), Mono Bus, Quad Bus — each adds instantly on pick. Keep the interaction one-click-then-pick; document the divergence from the reference's instant add in the release note. `BusBlock` channel labels already derive from `startChannel`; verify a quad bus renders 4 rows and cables 1→1..4→4 to a 4ch source (channelCablesFor uses min(channels) — no change needed, but add a geometry test for a 4ch pair).
3. **Docs**: configurations guide line for bus channel choice + release-notes bullet. `pnpm verify:docs` (update `scripts/verify-docs.sh` phrases if needed in the same commit).

## Verification

- `pnpm --filter @loopwire/desktop test && pnpm --filter @loopwire/desktop typecheck && pnpm verify:docs`
- Playwright smoke against `pnpm --filter @loopwire/desktop dev` on a free port (5175+): create device → bus menu → add Quad Bus → assert 4 channel rows and label "Channels 3–6". (Port 5173 is occupied by another project — do not kill it.)

## Commit

One commit `Add mono/stereo/quad bus creation`; update ROADMAP Phase 14 row + REQUIREMENTS BUS-01 + traceability; write `14.1-SUMMARY.md`.
