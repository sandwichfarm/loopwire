[package]
name = "loopwire"
version = "0.0.0"
description = "Linux virtual audio routing workspace"
authors = ["Loopwire contributors"]
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/sandwichfarm/loopwire"
build = "build.rs"

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
tauri = { version = "2.11.5", features = [] }
